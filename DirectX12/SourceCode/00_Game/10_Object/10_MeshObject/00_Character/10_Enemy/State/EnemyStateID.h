#pragma once

/**********************************************************************************
* @author    : mattya3713.
* @date      : 2026/08/11.
* @brief     : Enemyのステートを識別するID.
*            : 実装済みのステートのみを列挙する(未実装のIDを先に生やさない).
**********************************************************************************/

namespace EnemyState {

	enum class eID
	{
		None = 0, // 未初期化、または無効なステートID.

		Idle,   // 待機(ターゲットがAggroRange内に入るまで).
		Chase,  // 追跡(ターゲットへ直進).
		Attack, // 攻撃(足止めして攻撃判定を出す).
		Dead,   // 死亡(HPが0になった後. その場に残り続ける).

		_Max
	};

} // namespace EnemyState
